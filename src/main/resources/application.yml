spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/queryDsl
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  data:
    rest:
      max-page-size:
      default-page-size:
logging.level:
    org.hibernate.SQL: debug
